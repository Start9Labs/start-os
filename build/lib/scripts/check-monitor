#!/bin/sh

if which tvservice > /dev/null; then
    if tvservice -s | grep -qw off; then
        exit 1
    else
        exit 0
    fi
else
    if cat /sys/class/drm/*/status | grep -qw connected; then
        exit 0
    else
        exit 1
    fi
fi